version: 2

# conda:
#     environment: install/environment.yml

sphinx:
    builder: html
    configuration: docs/conf.py

build:
    image: latest

python:
    version: 3.7
    install:
      - requirements: docs/requirements.txt
